"""Optional framework adapters live here.

The core package intentionally stays framework-free. Add integrations as thin
wrappers around MemoryRuntime instead of coupling retrieval/storage to a
specific agent framework.
"""

